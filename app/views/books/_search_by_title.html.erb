<%= form_tag("/search_by_title", :method => "get") do %>
   <%= label_tag(:q, "Search for a Book Title:") %>
   <%= text_field_tag(:q, params[:q], :id => 'q_field') %>
   <%= submit_tag("Search") %>
<% end %>